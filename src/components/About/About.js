import React from 'react';
import Header from '../Header/Header';

const About = () => {
    return (
        <div>
            <Header />
            <h4>Thi is the true Real About me page</h4>
        </div>
    );
};

export default About;